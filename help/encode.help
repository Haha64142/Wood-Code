Usage: woodcode encode <text>
       woodcode encode [-f | --file] <file>

Encode a string or file of strings into WoodCode v1.3.5.

Arguments:
  <text>               The text you want to encode
  -f, --file <file>    Path to a file with one text entry per line
                       Each line will be encoded separately

Use 'woodcode decode' to decode a WoodCode string.